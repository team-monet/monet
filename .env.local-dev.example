# Isolated compose project name for long-lived local stack.
MONET_LOCAL_COMPOSE_PROJECT=monet

# PostgreSQL host bind for local machine access.
POSTGRES_DB=monet_local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=55432
POSTGRES_HOST=127.0.0.1

# API host bind for local machine access.
API_PORT=3301
PLATFORM_ADMIN_SECRET=change-me-local-admin-secret

# Dashboard host bind (only when using dashboard profile).
DASHBOARD_PORT=3310
NEXTAUTH_URL=http://localhost:3310
NEXTAUTH_SECRET=change-me-local-nextauth-secret
# Base64 encoding of exactly 32 bytes.
ENCRYPTION_KEY=MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=
DEV_BYPASS_AUTH=true
DASHBOARD_LOCAL_AUTH=true
# test-org in login form maps to LOCAL_TENANT_NAME when DASHBOARD_LOCAL_AUTH=true.

# API runtime.
ENRICHMENT_PROVIDER=ollama
ENRICHMENT_API_KEY=
EMBEDDING_API_KEY=
# Local compose Ollama endpoint.
OLLAMA_BASE_URL=http://ollama:11434
# Host Ollama model directory mounted into the container.
OLLAMA_MODELS_DIR=${HOME}/.ollama
# Recommended for Apple Silicon local quality/speed balance.
OLLAMA_CHAT_MODEL=qwen2.5:7b
# Must support 1536 output dimensions (Monet schema requirement).
OLLAMA_EMBEDDING_MODEL=qwen3-embedding:4b
OLLAMA_PORT=11434
AUDIT_RETENTION_DAYS=90
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# Local bootstrap defaults.
LOCAL_TENANT_NAME=Local Dev Org
LOCAL_GROUP_NAME=local-dev-default
LOCAL_AGENT_EXTERNAL_ID=local-dev-agent
LOCAL_DASHBOARD_EXTERNAL_ID=local-dashboard-admin
LOCAL_BOOTSTRAP_OUTPUT=.local-dev/bootstrap.json
LOCAL_BOOTSTRAP_CREATE_MEMORY=true

# Local usage metrics output path.
LOCAL_METRICS_OUTPUT=.local-dev/metrics.json
