#!/usr/bin/env bash
set -euo pipefail

if [[ "${MONET_SKIP_CI_GUARD:-0}" == "1" ]]; then
  echo "Skipping Monet CI guard because MONET_SKIP_CI_GUARD=1"
  exit 0
fi

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "${ROOT_DIR}"

echo "Running Monet CI guard before push..."
pnpm ci:local
