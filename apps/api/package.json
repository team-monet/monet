{
  "name": "@monet/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run --passWithNoTests",
    "test:unit": "vitest run --passWithNoTests --dir src",
    "test:integration": "vitest run --passWithNoTests --dir test"
  },
  "dependencies": {
    "@monet/db": "workspace:*",
    "@monet/mcp-tools": "workspace:*",
    "@monet/types": "workspace:*",
    "@hono/node-server": "^1",
    "hono": "^4"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "tsx": "^4",
    "typescript": "^5.7",
    "vitest": "^3"
  }
}
